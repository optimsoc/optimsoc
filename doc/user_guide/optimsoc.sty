\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{optimsoc}[2015/10/01 OpTiMSoC]

\RequirePackage{tikz}
\RequirePackage[framemethod=tikz]{mdframed}

\ProcessOptions\relax

% Note paragraphs
\newenvironment{docnote} {
  \begin{mdframed}[backgroundcolor=orange!30,linecolor=orange,frametitle={Note}]
}{
  \end{mdframed}
}

% Reference Boxes
\newcommand{\referencebox}[1]{%
  \tikz[overlay]
  \node[fill=black, inner sep=2pt, anchor=text, rectangle, rounded corners=1mm] {
    \color{white}#1 };
  \phantom{#1}
}

% OpTiMSoC name (in the right spelling)
\newcommand{\optimsoc}{OpTiMSoC}

\endinput
